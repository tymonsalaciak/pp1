"""
18.	An array contains values: 15, 8, 31, 47, 2, 19. Create a 
program that calculates and displays the array and the arithmetic 
mean of array values. Use the “for” loop statement.

1.	An array contains values: 15, 8, 31, 47, 2, 19. Create a program 
that calculates and displays the array and the arithmetic mean of 
array values. Use the “while” loop statement.
"""

arr = [15, 8, 31, 47, 2, 19]
"""
sum = 0
#arrAri = [i(i+i) ]

for i in arr:
    sum = sum + i
    x = int(len(arr))
# x tutaj odpowiada int(od ilosci elementó w arr)
#
Ari = sum/x
print(Ari)

"""
sum = 0
s =1
x = int(len(arr))
while s < x:
    for i in arr:
        sum = sum +i 
        s = s + 1
        sum

Ari = sum/x
print(Ari)

    
