arr = ['Genowefa',  'Onufry', 'Celestyna', 'Alojzy', 'Pankracy']


setcounetr = []
for i in arr:
        counter = len(i)
        setcounetr.append(counter)
        

maincounter = max(setcounetr)
print(maincounter)
        

# 8
#len(str(i.count(arr[0])))

"""
setcounetr = []
for i in arr:
        counter = len(i)
        setcounetr.append(counter)
        

maincounter = max(setcounetr)
print(maincounter)
"""
